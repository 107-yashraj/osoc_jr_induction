1. git init Demo - The init command will initialize an empty repository.  
2. git add Filename   - To add more than one file
3. git push [variable name] master   - This command sends the changes made on the master branch, to your remote repository.
4. git pull URL   - Pull command is used to receive data from GitHub. It fetches and merges changes on the remote server to your working directory.
5. git branch - This command lists all the branches available in the repository.  
6 git log   - This command is used to check the commit history.
7. git log -3   -  Git log shows the most recent commits first. We can limit the number of log entries displayed by passing a number as an option, such as -3 to show only the last three entries.
8. git reset - To unstage (remove) all files that have not been committed:
9. git reset HEAD~1 - To undo the most recent commit:
10. git merge <feature-branch> - When you are ready to add your changes to the default branch, you merge the feature branch into it:

Roll no :- 205121107
Name :- Yashraj Jain 
